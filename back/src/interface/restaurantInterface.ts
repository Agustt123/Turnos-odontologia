// src/interfaces/restaurant.interface.ts

interface Restaurant {
  id: number;
  name: string;
  openingHours: string;
  address: string;
  // Otras propiedades relevantes para un restaurante
}

export default Restaurant;
